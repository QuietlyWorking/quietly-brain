<script lang="ts">
	// Phase 3 stub. Renders the corpus entries with their reinforcement
	// history per preference (which sessions reinforced, redirected, last
	// reviewed). Lights up in Phase 3, once the corpus-update-on-redirect
	// writer ships.
</script>

<div
	class="rounded-sm border border-dashed border-border/70 bg-card/20 p-5 text-sm text-muted-foreground"
>
	<strong class="font-mono text-xs uppercase tracking-wider text-foreground"
		>Phase 3 placeholder</strong
	>
	<p class="mt-2">
		Named preferences with their reinforcement history. Each entry lists the sessions where the
		preference was reinforced or redirected, plus its last-reinforced timestamp for stale-corpus
		detection at the 90-day mark. Lights up in Phase 3, alongside the verdict mechanism.
	</p>
</div>
